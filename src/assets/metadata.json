{
    "couchUrl": "http://localhost:5984",
    "fields": [ "_id", "type", "channel", "parentId" ],
    "filters": [ {
        "field": "type", "operation": "$eq", "value": "", "name": "Type"
    }],
    "operations": {
        "$eq": "is",
        "$regex": "contains",
        "$gt": "greater than",
        "$lt": "less than"
    }
}